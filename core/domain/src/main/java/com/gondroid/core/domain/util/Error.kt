package com.gondroid.core.domain.util

/**
 * A marker interface is an empty interface—it has no methods—and it’s used to tag or classify classes that share a certain role or characteristic.
 */
interface Error